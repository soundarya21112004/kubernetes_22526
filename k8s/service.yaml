apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: production
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080 # Exposes the application on your EC2 Public IPs at port 30080